import styled from 'styled-components'

export const CompletedFormContent = styled.div`
  text-align: center;
  width: 50%;

  h1 {
    font-size: 3rem;
  }

  p {
    font-size: 1.25rem;
    margin: 1rem 0;
  }
`
